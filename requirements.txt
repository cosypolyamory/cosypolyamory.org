# Pinned so a rebuild can't silently change versions under us.
# Versions are those of the 2026-09-12 image, except peewee (see below).
#
# peewee is held at the final 3.x release. 4.x makes Model.delete a
# class-only descriptor, which breaks the attribute-copy loop in
# cosypolyamory/routes/events.py (500 on /events/). Upgrading to 4.x is a
# deliberate piece of work, not something a rebuild should do on its own.
flask==3.1.3
jinja2==3.1.6
flask-login==0.6.3
flask-session==0.8.0
peewee==3.19.0
authlib==1.8.0
requests==2.34.2
python-dotenv==1.2.3
faker==40.38.0
python-telegram-bot==22.8
schedule==1.2.2
pytz==2026.3.post1
itsdangerous==2.2.0
